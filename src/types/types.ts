export type TProduct = {
  _id: string;
  id: string;
  name: string;
  price: number;
  rating: number;
  brand: string;
  category: string;
  description: string;
  image: string;
  flashSale: boolean;
};
